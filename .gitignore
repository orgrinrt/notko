/target
Cargo.lock
.DS_Store
*.swp
*.swo
*.rs.bk

# The third-party discovery test writes a tier directory under the crate it
# runs in and removes it again. An interrupted run leaves one behind.
widget-tiers*/
